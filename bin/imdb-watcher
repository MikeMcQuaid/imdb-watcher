#!/usr/bin/env ruby
require 'pathname'
ROOT = File.expand_path("../../", Pathname.new(__FILE__).realpath)
ENV['BUNDLE_GEMFILE'] ||= "#{ROOT}/Gemfile"

require 'rubygems'
require 'bundler/setup'

load "#{ROOT}/imdb-watcher.rb"
